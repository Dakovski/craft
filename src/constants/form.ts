import type { FormProps } from "../types/form";

export const DEFAULT_FORM_VALUE: FormProps = {
  label: "",
  required: false,
  choices: [],
  displayAlpha: false,
  default: "",
};

export const FORM_VALUE_MAX_LENGTH = 40;

export const FORM_VALUEs_MAX_NUMBER = 50;
